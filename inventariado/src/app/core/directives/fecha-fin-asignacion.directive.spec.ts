import { FechaFinAsignacionDirective } from './fecha-fin-asignacion.directive';

describe('FechaFinAsignacionDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaFinAsignacionDirective();
    expect(directive).toBeTruthy();
  });
});
