import { FechaValidaDirective } from './fecha-valida.directive';

describe('FechaValidaDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaValidaDirective();
    expect(directive).toBeTruthy();
  });
});
